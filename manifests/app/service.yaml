apiVersion: v1
kind: Service
metadata:
  name: observability-demo
  namespace: observability-demo

spec:
  selector:
    app: observability-demo

  ports:
    - port: 80
      targetPort: 8000

  type: ClusterIP
